# Viata Luxe — Environment (copy to .env, never commit .env)
APP_ENV=production
APP_DEBUG=false
BASE_URL=https://viataluxe.com

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=viata_luxe
DB_USER=viata_user
DB_PASS=change-me-strong
DB_CHARSET=utf8mb4

# Optional: CDN base for assets (empty = same origin)
ASSET_URL=

# Session
ADMIN_TIMEOUT=1800
